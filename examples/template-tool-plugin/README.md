# template-tool-plugin

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

Deploy npm:

- Change the name of tool on package.json file
- Execute the command to deploy on npm:
```bash
npm publish --public
```
